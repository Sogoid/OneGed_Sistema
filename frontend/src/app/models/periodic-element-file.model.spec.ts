import { PeriodicElementFile } from './periodic-element-file.model';

describe('PeriodicElementFile', () => {
  it('should create an instance', () => {
    expect(new PeriodicElementFile()).toBeTruthy();
  });
});
