import { PeriodicElement } from './periodic-element-user.model';

describe('PeriodicElement', () => {
  it('should create an instance', () => {
    expect(new PeriodicElement()).toBeTruthy();
  });
});
